import { FilterAdminCategoryPipe } from './filter-admin-category.pipe';

describe('FilterAdminCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAdminCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
