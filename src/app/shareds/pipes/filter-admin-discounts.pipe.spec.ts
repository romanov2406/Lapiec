import { FilterAdminDiscountsPipe } from './filter-admin-discounts.pipe';

describe('FilterAdminDiscountsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAdminDiscountsPipe();
    expect(pipe).toBeTruthy();
  });
});
